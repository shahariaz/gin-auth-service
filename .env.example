# Environment Configuration
# Copy this file to .env and customize the values

# Application Configuration
GIN_MODE=debug                    # debug/release
PORT=8080
APP_VERSION=1.0.0

# JWT Configuration  
JWT_SECRET=your-super-secret-jwt-key-here

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting
RATE_LIMIT_PER_SEC=20

# Database Configuration
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=mydb
MYSQL_USER=myuser
MYSQL_PASSWORD=mypassword
DB_DSN=myuser:mypassword@tcp(localhost:3306)/mydb?charset=utf8mb4&parseTime=True&loc=Local

# Redis Configuration (optional - for production token blacklisting)
# REDIS_URL=redis://localhost:6379

# Development Notes:
# - Set GIN_MODE=debug to enable Swagger UI at /swagger/index.html
# - Set GIN_MODE=release for production deployment
# - Generate strong JWT_SECRET for production: openssl rand -base64 32
# - Use secure database credentials in production
# - Configure Redis for production token blacklisting
